<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 默认背景（灰色） -->
    <item 
        android:id="@android:id/background"
        android:drawable="@drawable/loading_del"
        >
        <shape>
            <corners android:radius="5dip" />

            <!-- <gradient
                android:angle="270"
                android:centerColor="@color/white"
                android:centerY="0.75"
                android:endColor="@color/white"
                android:startColor="@color/white" /> -->
        </shape>
    </item>

    <!-- 第二进度背景（黄色） -->
    <!-- <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:angle="270"
                    android:centerColor="#80ffb600"
                    android:centerY="0.75"
                    android:endColor="#80ffb600"
                    android:startColor="#80ffb600" />
            </shape>
        </clip>
    </item> -->

    <!-- 执行完的背景（蓝绿色） -->
    <item android:id="@android:id/progress"
        android:drawable="@drawable/loading_sel"
        >
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <!-- <gradient
                    android:angle="270"
                    android:centerColor="#39daca"
                    android:centerY="0.75"
                    android:endColor="#39daca"
                    android:startColor="#39daca" /> -->
            </shape>
        </clip>
    </item>

</layer-list>